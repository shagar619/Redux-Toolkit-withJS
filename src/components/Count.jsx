

const Count = ({ count }) => {
     return (
          <div>
               <div className="text-2xl font-semibold">
                    {count}
               </div>
          </div>
     );
};

export default Count;